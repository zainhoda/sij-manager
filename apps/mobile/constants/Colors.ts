/**
 * Re-export colors from the new theme system for backward compatibility.
 * New code should import directly from '@/theme' instead.
 */
export { default, lightTheme as light, darkTheme as dark } from '@/theme/colors';
